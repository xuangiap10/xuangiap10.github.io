add = function(x, y){
    return x+y;
}
subtract = function (x, y){
    return x - y;
}
multiply = function (x, y){
    return x*y;
}
divide = function (x, y){
    return x/y;
}
pi = 3.14; // constant set to 3.14

module.exports = {
    add: add,
    subtract: subtract,
    multiply: multiply,
    divide: divide,
    pi: pi
};